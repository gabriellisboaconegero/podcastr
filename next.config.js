// Usa esse config para poder dizer que as imagens que vão ser pegas estão sendo hoteadas nesse dominio
// ao usar o component Image do Next, é preciso usar esse config


module.exports = {
    images: {
        domains: ['storage.googleapis.com']
    }
}